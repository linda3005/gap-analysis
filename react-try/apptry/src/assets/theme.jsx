import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#ffffff', // Ganti dengan warna yang diinginkan
    },
    secondary: {
      main: '#ffffff', // Ganti dengan warna yang diinginkan
    },
    mode: "dark"
  },
});

export default theme;